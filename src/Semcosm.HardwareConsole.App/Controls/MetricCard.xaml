<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Semcosm.HardwareConsole.App.Controls.MetricCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Name="Root">

    <Border
        Width="{x:Bind Root.CardWidth, Mode=OneWay}"
        MinHeight="{x:Bind Root.MinCardHeight, Mode=OneWay}"
        Padding="20"
        CornerRadius="16"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

        <StackPanel Spacing="10">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="8">
                    <TextBlock
                        Text="{x:Bind Root.Title, Mode=OneWay}"
                        FontSize="20"
                        FontWeight="SemiBold" />

                    <TextBlock
                        Text="{x:Bind Root.Subtitle, Mode=OneWay}"
                        Opacity="0.7"
                        TextWrapping="Wrap" />
                </StackPanel>

                <Border
                    Grid.Column="1"
                    Padding="10,4"
                    CornerRadius="999"
                    Background="{ThemeResource LayerFillColorDefaultBrush}">
                    <TextBlock
                        Text="{x:Bind Root.Status, Mode=OneWay}"
                        FontSize="12"
                        FontWeight="SemiBold"
                        Opacity="0.86" />
                </Border>
            </Grid>

            <TextBlock
                Text="{x:Bind Root.PrimaryValue, Mode=OneWay}"
                FontSize="{x:Bind Root.PrimaryFontSize, Mode=OneWay}" />

            <TextBlock
                Text="{x:Bind Root.SecondaryValue, Mode=OneWay}"
                Opacity="0.7"
                TextWrapping="Wrap" />
        </StackPanel>
    </Border>
</UserControl>
