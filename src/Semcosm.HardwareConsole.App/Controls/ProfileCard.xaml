<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Semcosm.HardwareConsole.App.Controls.ProfileCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Semcosm.HardwareConsole.App.Controls"
    xmlns:local="using:Semcosm.HardwareConsole.App.Controls"
    x:DataType="local:ProfileCard"
    x:Name="Root">

    <Border
        Padding="20"
        CornerRadius="16"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

        <StackPanel Spacing="16">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="8">
                    <TextBlock
                        Text="{x:Bind DisplayName, Mode=OneWay}"
                        FontSize="22"
                        FontWeight="SemiBold" />

                    <TextBlock
                        Text="{x:Bind Description, Mode=OneWay}"
                        TextWrapping="Wrap"
                        Opacity="0.76" />
                </StackPanel>

                <StackPanel Grid.Column="1" Spacing="10">
                    <controls:RiskBadge RiskLevel="{x:Bind RiskLevel, Mode=OneWay}" />
                    <controls:CapabilityChip
                        x:Name="ActiveChip"
                        Text="Active"
                        x:Load="{x:Bind IsActive, Mode=OneWay}" />
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <controls:CapabilityChip Text="{x:Bind SourceText, Mode=OneWay}" />
                <controls:CapabilityChip Text="{x:Bind CapabilityCountText, Mode=OneWay}" />
                <controls:CapabilityChip Text="{x:Bind ActionCountText, Mode=OneWay}" />
                <controls:CapabilityChip Text="{x:Bind PolicyCountText, Mode=OneWay}" />
                <controls:CapabilityChip
                    x:Name="ConfirmationChip"
                    Text="{x:Bind ConfirmationText, Mode=OneWay}"
                    x:Load="{x:Bind ShowConfirmation, Mode=OneWay}" />
            </StackPanel>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"
                    Content="Preview"
                    Click="PreviewButton_Click" />

                <Button
                    Grid.Column="1"
                    Content="Apply"
                    Click="ApplyButton_Click" />
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
