<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Semcosm.HardwareConsole.App.Controls.FanCurvePointRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Semcosm.HardwareConsole.App.Controls"
    x:DataType="local:FanCurvePointRow"
    x:Name="Root">

    <Border
        Padding="12"
        CornerRadius="12"
        Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Text="{x:Bind InputText, Mode=OneWay}"
                FontWeight="SemiBold" />

            <TextBlock
                Grid.Column="1"
                Text="{x:Bind OutputText, Mode=OneWay}"
                Opacity="0.76" />
        </Grid>
    </Border>
</UserControl>
